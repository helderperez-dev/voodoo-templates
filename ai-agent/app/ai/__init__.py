"""The AI layer — just the ``@tool`` functions the agent can call.

The agent itself (and its model config) lives in ``main.py``; providers are
resolved by the framework from the ``[ai]`` block in ``voodoo.toml``.
"""
