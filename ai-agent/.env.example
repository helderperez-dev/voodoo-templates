# Copy this file to .env and fill in real values. `.env` is git-ignored.
#
# Works with DeepSeek or any OpenAI-compatible endpoint (OpenRouter, Ollama,
# vLLM, LiteLLM gateways...). The model id itself lives in voodoo.toml [ai].
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
